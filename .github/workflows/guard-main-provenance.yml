# Tap repo guard-main-provenance wrapper — calls the centralized reusable workflow.
#
# Verifies commits in PRs to main came from squash-merged PRs to dev
# (have (#N) references). Only needs pull-requests: read.
name: Guard main commit provenance

on:
  pull_request:
    branches: [main]

permissions:
  pull-requests: read

concurrency:
  group: guard-main-provenance-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  guard-provenance:
    uses: brettdavies/.github/.github/workflows/guard-main-provenance.yml@main
